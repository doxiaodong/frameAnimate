{
  "name": "frameAnimate.js",
  "version": "2.0.0",
  "scripts": {
    "build": "babel src -d lib && babel src -d docs"
  },
  "devDependencies": {
    "babel-cli": "^6.0.0",
    "babel-preset-es2015": "^6.9.0"
  }
}
